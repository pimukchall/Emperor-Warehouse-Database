{ 
    "version": 2,
    "builds": [
        {
            "src": "src/index.ts",
            "use": "@now/node",
            "config": { "maxLambdaSize": "10mb" }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "src/index.ts" 
        }
    ]
}